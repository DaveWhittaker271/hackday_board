type get_ideas_response {
  ideas: [idea]
}

type idea {
  id: Int!
  user_id: Int!
  project_id: Int!
  title: String!
  description: String
  user_name: String
  user_image: String
  timecreated: Int
}

extend type Query {
  get_ideas(
    project_id: Int!
  ): get_ideas_response
}