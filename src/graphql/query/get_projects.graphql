query (
    $id: Int
) {
    get_projects(
        id: $id
    ) {
        projects {
            id
            title
            description
            submitted_by_name
            picture_url
            files
        }
    }
}